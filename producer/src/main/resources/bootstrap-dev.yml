spring:
  application:
    name: producer
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
      
eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false
